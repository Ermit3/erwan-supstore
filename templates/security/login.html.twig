{% extends 'base.html.twig' %}

{% block title %}Log in!
{% endblock %}

{% block carousel %}{% endblock %}

{% block body %}
	<div class="container" style="padding:10% 2%;">
		<div class="row">
			<div class="col-md-4">
				<form method="post">
					{% if error %}
						<div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
					{% endif %}

					{% if app.user %}
						<div class="mb-3">
							Vous etes connectez en tant que
							{{ app.user.username }},
							<a href="{{ path('security_logout') }}">Logout</a>
						</div>
					{% endif %}

					<h1 class="h3 mb-3 font-weight-normal">Se connecter</h1>
					
					<div class="form-outline mb-3">
						<input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control" autocomplete="email" required autofocus>
						<label class="form-label" for="inputEmail">Adresse mail</label>
					</div>
					<div class="form-outline mb-3">
					<input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>
					<label class="form-label" for="inputPassword">Mot de passe</label>
					</div>

					<input
					type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

					{# Uncomment this section and add a remember_me option below your firewall to activate remember me functionality. See https://symfony.com/doc/current/security/remember_me.html <div class="checkbox mb-3"> <label> <input type="checkbox" name="_remember_me"> Remember me </label> </div> #}

					<!-- 2 column grid layout for inline styling -->
					<div class="row mb-4">
						<div class="col d-flex justify-content-center">
						<!-- Checkbox -->
							<div class="form-check">
								<input class="form-check-input" type="checkbox" value="" id="form1Example3" checked />
								<label class="form-check-label" for="form1Example3"> Se souvenir de moi </label>
							</div>
						</div>

						<div class="col">
							<!-- Simple link -->
							<a href="#!">Mot de passe oublié ?</a>
						</div>
					</div>

					<div class="text-center">
						<button class="btn btn-lg btn-primary" type="submit">
							Connexion
						</button>
					</div>

				</form>
			</div>
			<div class="col-md-2"></div>
			<div class="col-md-6">
				<h2>S'incrire</h2>
				<p>Lorem, ipsum dolor sit amet consectetur adipisicing elit. Suscipit et numquam mollitia quos. Delectus laborum natus nulla, deleniti, illo dicta maiores veniam, sapiente asperiores odio corrupti doloremque pariatur eius numquam! Nisi ipsa cum provident saepe, quibusdam, fuga similique iusto distinctio quaerat explicabo alias et accusantium dolores eius tenetur possimus numquam?</p>
				<div class="text-center">
					<button class="btn btn-lg btn-success">
						S'incrire
					</button>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
